"""Project scaffolding: conversational init -> manifest -> instrumented product.

- manifest.py  — the extended init manifest (what/goal/benchmarks/niche + roles),
                 validation, and the plain-language charter confirmation.
- templates.py — instrumentation-complete product skeletons (the "bones" that
                 guarantee telemetry works no matter what agents build on top).
- builder.py   — render a manifest into a real project directory on disk.
"""
